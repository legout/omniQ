## 1. Spec
- [ ] Update `omniq-api-config` to reflect the chosen `enqueue` API shape
- [ ] Add scenarios for callable enqueue and (if supported) explicit `func_path` enqueue

## 2. Documentation
- [ ] Update `README.md` quick start and task management examples to match the implemented API
- [ ] Update any `examples/` code to match

## 3. Validation
- [ ] Run `openspec validate update-public-api-docs --strict`

